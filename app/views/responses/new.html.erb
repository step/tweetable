<div class="container-fluid container">
  <div id="timeout_alert">
  </div>
  <div class="row">
    <div class="col-lg-11" style="margin-bottom: 50px; ">
      <%= content_tag :h3, :class => 'text-left' do %>
          <%= passage.title %>
          <%= content_tag(:span, 'pull-right') do %>
              <h4 class="pull-right" >Remaining Time: <span class="count" id="app-remaining-time"><%= remaining_time %></span></h4>
          <% end %>
      <% end %>
      <div class="app-passage-holder">
        <%= simple_format(passage.text) %>
      </div>
    </div>
    <%= render 'form', response: response, passage: passage, user: user %>
  </div>
</div>
