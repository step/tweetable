<%= stylesheet_link_tag 'application' %>
<%= javascript_include_tag 'application' %>

<%= render :partial => "shared/top_nav_bar" %>
<div class=" container">
  <div class="panel panel-success clearance">
    <div class="panel-heading"><h4>Thank you, <%= current_user.name %> for signing up.</h4></div>
    <div class="panel-body">
      <p>Your account will be activated once the admin authorizes and assigns your role.</p>
    </div>
  </div>

</div>
