<div class="container">
  <h2>Leader Board</h2>
  <table class="table table-striped">
    <thead>
    <tr>
      <th>Name</th>
      <th>Rank</th>
    </tr>
    </thead>
    <tbody>

    <% @leader_list.each do |user| %>
        <tr>
          <td><img class="user-image" src=<%= user[:image] %>><%= user[:name] %></td>
          <td><%= user[:rank] %><span class="points"> ( Points: <%= user[:points] %> )</span></td>
        </tr>
    <% end %>
    </tbody>
  </table>
</div>